# shinkoku

日本の確定申告を支援するプラグインです。Python 3.11+、CLIのJSON入出力、SQLiteを使います。計算や帳簿処理は既存CLI、作成コーナーへの入力は利用可能なブラウザツールで行います。モデルやブラウザ名を過去の文書から固定しません。

## 常に守る境界

- 金額は円単位の `int`。浮動小数で金額計算をしません。税率・端数処理・対象年度・入出力契約を勝手に変えません。
- DBはWALと外部キーを維持し、既存の接続・初期化関数を使います。ユーザーの帳簿を削除・上書き・初期化し直しません。
- 実帳簿への仕訳登録・修正・削除、税務上の申告方法の確定は、対象内容へのユーザー確認を経て行います。同じ内容が既に確認済みなら再承認を求めません。読み取り、試算、一時出力、テストデータの処理は依頼範囲で進めます。
- e-Taxの本人認証、マイナンバー入力、電子署名・最終送信はユーザー操作です。入力内容の目視確認と送信前の意思決定ゲートを維持し、準備完了を送信済みと扱いません。
- 個人情報を含む設定・DB・原本・`.data`・進捗・`output/` を公開・コミットしません。設定テンプレートは `shinkoku.config.example.yaml`。秘密の値や認証情報をログへ出しません。
- サブエージェントはこの会話で明示的に依頼された場合だけです。OCRの二重読み取りやブラウザ機能の存在だけを起動の許可にしません。

## 必要な情報への入口

- 対応する所得・申告の範囲: `skills/capabilities/SKILL.md`。対象外の申告を対応済みとせず、専門家へ案内します。
- 実際の申告支援: `skills/` の該当Skill。全Skillを読む必要はありません。一般的な制度説明だけなら設定ファイルや全進捗の読込は不要です。
- 計算・CLI・DBのコード変更、コミット・PR: `docs/development-contracts.md` の該当節。型やコマンドの正本はコードと `tests/fixtures/cli_contract.json` です。
- 年度別の税制: 該当Skillの参照資料。資料の年度を確認し、別年度への適用や未確認の法改正を推測しません。

## 開発の完了

既存の変更を保持し、依頼範囲の実装・実行・検証・修正・再検証まで進めます。コードは `make test` / `make lint` または対応する `uv run` の関連検査を使います。Skill内のコマンド変更・移動は `tests/unit/test_skill_cli_contract.py` でCLI契約も確かめます。検査不能は理由と未確認範囲を報告し、実ユーザーの申告や帳簿をテストに使いません。
